# FLN — Assessment Answer Key & Template Generator

This branch builds the **AI-powered Assessment Answer Key & Template Generator** feature only.

## Folder layout

| Folder | Purpose |
|---|---|
| `frontend/`  | React + Vite UI (upload PDF → review → approve) |
| `backend/`   | Node.js API + Python FastAPI automation (Gemini vision) |
| `SRS.md`     | Upstream Software Requirements Specification |

## Status

- [x] Branch scaffolded with `frontend/` + `backend/` folders
- [ ] Pending: paste real source files into the two folders